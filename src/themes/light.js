import { createTheme } from "@mui/material/styles";

const Light_theme = createTheme({
     palette: {
          mode: "light",
          primary: {
               main: "#0188D3",
          },
          secondary: {
               main: "#9c1c1e",
          },
     },
});

export default Light_theme;
