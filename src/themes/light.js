import { createTheme } from "@mui/material/styles";

const Light_theme = createTheme({
     palette: {
          mode: "light",
          primary: {
               main: "#0188D3",
          },
          secondary: {
               main: "#520000",
          },
     },
});

export default Light_theme;
