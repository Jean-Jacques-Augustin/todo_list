import { createTheme } from "@mui/material/styles";

const Dark_theme = createTheme({
     palette: {
          mode: "dark",
          primary: {
               main: "#3949ab",
          },
          secondary: {
               main: "#d32f2f",
          },
     },
});

export default Dark_theme;
